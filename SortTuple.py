#sorting tuples using the sorted method
Example = tuple()
#create a list
Example = { 'b':2, 'd':4, "a":1, 'c':3}
#create a list of tuples inoder to sort them. since tuples can be sorted
listT = sorted(Example.items())
print(listT)
